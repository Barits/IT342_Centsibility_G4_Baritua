# Production Profile
spring.config.activate.on-profile=prod

# Database
spring.datasource.url=jdbc:mysql://production-server:3306/centsibility_prod?useSSL=true&serverTimezone=UTC
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Logging
logging.level.org.springframework.security=WARN
logging.level.com.centsibility=INFO
